# Project visual assets

Project images are optional for User Story 1. When a project has no `image` or
`details.screenshotsOrDiagrams` entry, the UI intentionally omits image markup so
no broken placeholder is rendered.

Add optimized screenshots or diagrams here and reference them from
`src/data/projects.ts` using Vite-supported asset paths.
